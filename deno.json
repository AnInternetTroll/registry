{
	"fmt": {
		"options": {
			"useTabs": true
		},
		"files": {
			"exclude": [
				"coverage"
			]
		}
	},
	"tasks": {
		"start": "#!/usr/bin/env -S deno run --allow-net --allow-write=./repos_main --allow-read=. --no-prompt --allow-env main.ts",
		"dev": "#!/usr/bin/env -S deno run --allow-net --allow-write=./repos_main --allow-read=. --no-prompt --allow-env --watch main.ts",
		"check": "deno fmt --check && deno lint && deno check main.ts",
		"test": "deno test --allow-read=./repos_test,.env.example --allow-write=./repos_test --allow-net=github.com --allow-env --coverage=coverage --no-check -- --env-file=.env.example && deno coverage coverage --lcov > lcov.info"
	}
}
